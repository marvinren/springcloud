export class Optype {
    op_type_id: number;
    op_type_name: string; // 名称
    code: string;  // 编号

    notes: string; // 备注
    state: number ; // 状态

    operate_type: string;
    readonly: boolean;
}